<form method="post" action="{{ path('app_admin_category_delete', {'id': admin_category_controller.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin_category_controller.id) }}">
    <button class="btn">Delete</button>
</form>
