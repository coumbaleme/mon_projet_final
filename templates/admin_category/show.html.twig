{% extends 'base.html.twig' %}

{% block title %}AdminCategoryController{% endblock %}

{% block body %}
    <h1>AdminCategoryController</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin_category_controller.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_category_index') }}">Retour à la liste</a>

    <a href="{{ path('app_admin_category_edit', {'id': admin_category_controller.id}) }}">Modifier</a>

    {{ include('admin_category/_delete_form.html.twig') }}
{% endblock %}
