<form method="post" action="{{ path('app_category_projet_delete', {'id': category_projet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_projet.id) }}">
    <button class="btn">Delete</button>
</form>
