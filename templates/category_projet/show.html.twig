{% extends 'base.html.twig' %}

{% block title %}CategoryProjet{% endblock %}

{% block body %}
    <h1>CategoryProjet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_projet.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_projet_index') }}">back to list</a>

    <a href="{{ path('app_category_projet_edit', {'id': category_projet.id}) }}">edit</a>

    {{ include('category_projet/_delete_form.html.twig') }}
{% endblock %}
