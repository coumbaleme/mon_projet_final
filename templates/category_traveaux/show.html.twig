{% extends 'base.html.twig' %}

{% block title %}CategoryTraveaux{% endblock %}

{% block body %}
    <h1>CategoryTraveaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_traveaux.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_traveaux_index') }}">back to list</a>

    <a href="{{ path('app_category_traveaux_edit', {'id': category_traveaux.id}) }}">edit</a>

    {{ include('category_traveaux/_delete_form.html.twig') }}
{% endblock %}
