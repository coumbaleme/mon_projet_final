{# templates/base.html.twig #}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}BTP_Consulting{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="BTP_Consulting - Votre partenaire de confiance
    {# <link rel="stylesheet" href="{{ asset('styles.css') }}"> #}

    <link rel="icon" href="{{ asset('images/logo.jpg') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
    
    {% block stylesheets %}{% endblock %}
    <style>
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        main {
            flex: 1;
        }
    </style>
</head>
<body>

<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_homepage') }}">
                <img src="{{ asset('images/logo.jpg') }}" alt="BTP_Consulting" width="50">
            </a>

            <ul class="navbar-nav me-auto">
                
                <li class="nav-item"><a class="nav-link" href="{{ path('app_accueil') }}">Accueil</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_profile') }}">Profil</a></li>
               <li class="nav-item"><a class="nav-link" href="{{ path('app_apropos') }}">A propos</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_contact') }}">Contact</a></li>

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_admin_user_index') }}">Admin</a></li>
                {% endif %}
            </ul>

            <div class="d-flex align-items-center">
                <a href="{{ path('panier') }}" class="btn btn-outline-light me-2">🛒</a>

                {% if app.user %}

                {% else %}
                    <a href="{{ path('app_inscription') }}" class="btn btn-outline-light me-2">S’inscrire</a>
                    <a href="{{ path('app_login') }}" class="btn btn-warning">Connexion</a>
                {% endif %}
            </div>
        </div>
    </nav>

    {# Barre Admin visible seulement pour les administrateurs #}
    {% if is_granted('ROLE_ADMIN') %}
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('app_admin_user_index') }}">Admin Panel</a>
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">Pages Admin</a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('app_admin_user_index') }}">Utilisateurs</a></li>
                             <li><a class="dropdown-item" href="{{ path('product_index')|default('#') }}">Produits</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_category_traveaux_index')|default('#') }}">Travaux</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_category_metier_index')|default('#') }}">Métiers</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_category_projet_index')|default('#') }}">Projets</a></li>
                        </ul>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('app_logout') }}" class="btn btn-warning">Se déconnecter</a>
                    </li>
                </ul>
            </div>
        </nav>
    {% endif %}
</header>

<main class="container mt-4">
    {% block body %}{% endblock %}
</main>

<footer class="bg-dark text-light mt-5 py-4">
    <div class="container text-center">
        <p>&copy; 2025 BTP_Consulting — Tous droits réservés</p>
    </div>
</footer>

<script src="{{ asset('assets/js/cookie.js') }}"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
